#include <iostream> 
#include <bits/stdc++.h>
#include <vector>
using namespace std ;

int main(){
    vector<string> exercise ;
    string s ;

    for(int i = 0 ; i<7 ; i++){
        getline(cin , s);
        exercise.push_back(s);
    }
    for(const strings day : exercise ){
        int n = day.length();
        string num = "";
        for(int i = n-1 ; i >= 0 ; i)
    }
}